# RoboFlow Inference SDK Set Up


## Install Pip

If you have not installed pip
`py -m ensurepip --upgrade`

## Install packages

Install the pip packages for this project
`pip install -r requirements.txt`

## Run app

Run the app in the command line
`uvicorn app.main:app --reload`
